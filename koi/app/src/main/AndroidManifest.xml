<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jeongmin.koi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".functionActivity" />
        <activity android:name=".translateActivity" />
        <activity android:name=".customActivity" />
        <activity android:name=".addSoundActivity" />
        <activity android:name=".soundListActivity" />
        <activity android:name=".busModeActivity" />
        <activity android:name=".getBusActivity" />
        <activity android:name=".getBusActivity2" />
        <activity android:name=".getBusActivity3" />
        <activity android:name=".getBusActivity4"></activity>
    </application>

</manifest>